# 통합 산출물 요청

> 통합 배포 URL: https://steam-domain-specific-llm.vercel.app/

> 개인 작업 마감: 2026-07-23(목)  

## 개요

안녕하세요. 최종 데모에서 네 파트를 한 화면으로 연결하기 위한 API 형식을 정리했습니다.

각자 모델이나 내부 코드는 바꿀 필요 없고, 아래 산출물만 부탁드립니다. 공통 연결 키는 Steam 원본 `appid`입니다.

- **B 리뷰**: 배포한 B 서버의 `GET /panel` base URL이 필요합니다. `appid`별 긍정/부정 토픽과 집계 리뷰 수를 반환해 주세요.
- **C 동적추천**: `GET /search`와 `GET /panel`이 필요합니다. 프론트가 보내는 익명 `uid`를 기준으로 현재 성향 추천과 해당 게임의 추천 근거를 반환합니다.
- **D 조건검색**: `GET /search`가 필요합니다. 질의에서 추출한 조건은 응답 `note`, 게임별 충족 근거는 각 결과의 `reason`에 담아 주세요.

C/D 검색은 A와 동일하게 `k=30`으로 요청합니다. 서버는 조건에 맞는 게임을 최대 30개 반환하고, 화면은 우선 10개를 보여준 뒤 `더 보기`로 30개까지 펼칩니다.

여기서 base URL은 우리 게이트웨이 주소가 아니라 **각자 배포한 자기 파트 서버 주소**입니다. 목표는 목요일(7/23)까지 B/C/D의 공개 HTTPS base URL을 받는 것입니다. 배포가 막히거나 현재 출력 구조가 다르면 **실제 출력 예시 1개, 막힌 지점** 등을 보내 주세요.

## 공통 규칙

### 요청값 뜻

| 값 | 뜻 |
|---|---|
| `appid` | Steam 원본 게임 ID. |
| `q` | 사용자가 입력한 원문 검색어. C는 빈 문자열 허용, D는 필수 |
| `k` | 최대 결과 수. A/C/D 모두 30으로 통일 |
| `uid` | 프론트가 만든 익명 사용자 ID. C는 사용, B/D는 받아서 무시 가능 |

`<게임ID>`, `<선택>` 같은 표기는 실제 문자열이 아니라 값을 넣을 자리입니다. URL의 `%20`은 띄어쓰기입니다.

### 검색 응답 공통 규칙 — C·D

```json
{
  "results": [
    {"appid": 123, "name": "게임명", "score": 0.91, "reason": "추천 근거"}
  ],
  "note": "검색 전체에 적용되는 현재 성향 또는 추출 조건"
}
```

- `appid`, `name`, `reason`은 필수입니다.
- `score`는 권장 숫자이며 가능하면 0~1로 주세요. 없으면 화면에서 점수 배지를 생략합니다.
- `k=30`은 정확히 30개가 아니라 **최대 30개**입니다. 조건에 맞는 게임이 7개면 7개만 반환합니다.
- 1-10개면 전부 바로 표시하고, 11-30개면 처음 10개 후 `더 보기`로 나머지를 표시합니다.
- 결과가 없으면 HTTP 200으로 `{"results": [], "note": "조건에 맞는 게임 없음"}`을 반환합니다.
- 개수를 맞추려고 조건에 맞지 않는 게임을 채우지 않습니다.

### 패널 응답 공통 규칙 — B·C

```json
{
  "status": "ok",
  "fields": [{"k": "화면 라벨", "v": "화면에 표시할 값"}],
  "note": ""
}
```

- `status`는 `ok`, `unavailable`, `error` 중 하나입니다.
- `fields`의 `k`, `v`는 모두 화면에 바로 표시되는 문자열입니다.
- 해당 게임 데이터가 없으면 HTTP 오류 대신 `{"status":"unavailable","fields":[],"note":"데이터 없음"}`을 반환합니다.
- C/D 검색의 게이트웨이 강제 타임아웃은 A 검색과 동일한 120초입니다. B/C 패널은 A 패널과 동일하게 요청당 25초이며, 타임아웃 또는 예열 응답일 때 화면이 자동 재시도합니다. 각 API는 이 제한 안에서 가능한 한 빠르게 응답해 주세요.

### 공통 상태 확인

각 서버에 모델을 새로 실행하지 않는 가벼운 상태 확인 API를 추가해 주세요.

```http
GET /health
```

```json
{"status": "ok"}
```

## B 파트

B 파트는 리뷰별 16개 토픽 결과를 최종 화면에서 게임별로 보여주려고 합니다.

배포한 서버의 아래 API를 기본으로 부탁드립니다.

```http
GET /panel?appid=<게임ID>&q=<무시가능>&uid=<무시가능>
```

응답 예시:

```json
{
  "status": "ok",
  "fields": [
    {"k": "긍정 토픽", "v": "gameplay, replayability"},
    {"k": "부정 토픽", "v": "difficulty"},
    {"k": "표본 리뷰 수", "v": "1,842건"}
  ],
  "note": ""
}
```

확인하고 싶은 것은 다음 네 가지입니다.

1. 현재 결과에 Steam `appid`가 연결되어 있는지
2. 리뷰 단위 예측을 게임 단위 top 긍정/부정 토픽으로 집계할 수 있는지
3. `/panel` 실제 응답 1개
4. 외부에서 호출 가능한 공개 HTTPS base URL

## C 파트

C 파트는 **C가 직접 추천한 목록**을 C 탭에 보여주고, 그 결과를 눌렀을 때만 성향 근거 패널을 붙이는 구조로 잡았습니다.

필요한 API는 같은 base URL 아래 두 개입니다.

```http
GET /search?q=<선택>&k=30&uid=<익명ID>
GET /panel?appid=<게임ID>&q=<선택>&uid=<동일ID>
```

`/search` 예시:

```json
{
  "results": [
    {"appid": 1245620, "name": "ELDEN RING", "score": 0.87,
     "reason": "최근 액션 RPG 만족도가 높아지고 오래된 캐주얼 선호가 감쇠했습니다."},
    {"appid": 1086940, "name": "Baldur's Gate 3", "score": 0.83,
     "reason": "최근 서사와 선택 중심 플레이 선호가 높아진 흐름과 일치합니다."},
    {"appid": 1145360, "name": "Hades", "score": 0.78,
     "reason": "반복 도전과 빠른 액션에 대한 현재 선호도가 높습니다."}
  ],
  "note": "현재 성향: 탐험·고난도 전투 선호 / 최근 경험 가중"
}
```

확인하고 싶은 것은 다음 다섯 가지입니다.

1. 실제 응답 예시 (`/search`, `/panel`)
2. 성향이 내부 시간, 호출, 플레이 피드백 중 무엇으로 변하는지
3. `uid`별 상태를 저장할 수 있는지
4. 발표에서 성향 변화를 즉시 보여줄 프리셋 `uid` 또는 상태 전환 방법이 있는지
5. 외부에서 호출 가능한 공개 HTTPS base URL

## 파트 D

D 파트는 **자연어 질의→조건 추출→필터/재정렬→게임 목록**을 D 탭에 연결합니다. 게임 상세 패널은 만들지 않습니다.

필요한 API는 하나입니다.

```http
GET /search?q=무료%20멀티%20공포&k=30&uid=<무시가능>
```

응답 예시:

```json
{
  "results": [
    {"appid": 700330, "name": "SCP: Secret Laboratory", "score": 0.92,
     "reason": "무료·멀티플레이 조건을 모두 충족하고 공포 태그 관련도가 높습니다."},
    {"appid": 224260, "name": "No More Room in Hell", "score": 0.88,
     "reason": "무료 협동 멀티플레이와 좀비 공포 분위기 조건에 부합합니다."},
    {"appid": 223710, "name": "Cry of Fear", "score": 0.82,
     "reason": "무료 게임이며 협동 플레이와 심리 공포 요소를 포함합니다."}
  ],
  "note": "추출 조건: 무료 / Multi-player / 자유 텍스트=공포"
}
```

- `note`: LLM이 실제 추출한 조건 요약
- `reason`: 각 게임이 조건을 충족한 근거
- `appid`, `name`, `reason`은 꼭 포함

확인하고 싶은 것은 다음 두 가지입니다.

1. 실제 응답 예시 (`/search`)
2. 외부에서 호출 가능한 공개 HTTPS base URL

## A 파트가 받은 뒤 할 일

1. B/C/D의 공개 HTTPS base URL 수령
2. 각 서버 응답 형식 검증
3. 게이트웨이에 base URL 환경변수 연결
4. A/C/D 검색 탭과 A/B/C 통합 상세 화면에서 실제 데이터 확인
5. 느림·미제공·서버 중단 시 파트별 fallback 확인
6. 팀원에게 통합 화면 캡처와 수정 필요사항 회신
